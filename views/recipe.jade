extends layout

block content
  .container(ng-controller="RecipeCtrl")
    h1= title
    p Welcome to #{title}
    form.form-inline(role="form")
      div.form-group
        label.sr-only(for="ingredient") Ingredient Name
        input.form-control(type="text",placeholder="Enter Ingredient Name",type-ahead)


    table.table.table-striped
      tr
        th Qty
        th Measure
        th Ingredient
        th Cal
        th &nbsp;
      //- tr.success
      //-   td 
      //-     input(type="text",value="1",size="2")
      //-   td 
      //-     select
      //-       option cup
      //-       option tbsp
      //-   td Celery, raw 
      //-   td Y   
      //-   td 
      //-     button.btn.btn-default(type="submit") Save

      tr(ng-repeat="item in items")
        td {{item.nf_serving_size_qty}}
        td {{item.nf_serving_size_unit}}
        td {{item.item_name}}
        td {{item.nf_calories}}
        td [x]

